{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel index.html --no-cache",
    "build": "parcel build index.html --no-cache"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
